<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cuncis.pemesanantiketonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.EditProfileActivity" />
        <activity android:name=".ui.MyProfileActivity" />
        <activity android:name=".ui.SuccessBuyTicketActivity" />
        <activity android:name=".ui.TicketCheckoutActivity" />
        <activity android:name=".ui.TicketDetailActivity" />
        <activity android:name=".ui.SuccessRegisterActivity" />
        <activity android:name=".ui.GetStartedActivity" />
        <activity android:name=".ui.RegisterTwoActivity" />
        <activity android:name=".ui.RegisterOneActivity" />
        <activity android:name=".ui.SigninActivity" />
        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.HomeActivity" />
    </application>

</manifest>